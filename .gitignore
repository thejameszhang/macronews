# Secrets and credentials
secrets/
.env

# Virtual environment
.venv/

# Data (large, not tracked)
data/
datasets/

# Logs and results
logs/
results/
docs/

# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/
.coverage

# IDE / editor
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Claude
.claude/
CLAUDE.md
claude.sh
article_cache/

# LaTeX build artifacts
*.aux
*.log
*.nav
*.snm
*.toc
*.vrb
*.fls
*.fdb_latexmk
*.synctex.gz

# Personal notes / design docs / slides — kept local, not tracked
ARCHITECTURE.md
design/
slides/
audits/

# Local-only ad-hoc scripts (one-off audits, scratch work)
scripts/misc/

# Autoresearch sandbox (kept local, not tracked)
autoresearch/
# Docs are a deliverable, not shipped code, and live only on the mapper-docs
# branch (worktree at ../macronews-docs). Ignored here so a `git add -A` on main
# can never sweep in LaTeX build artifacts or the doc sources themselves.
documentation/

# Local-only analysis / reporting code (never shipped with the package)
# /analysis/ -- tree deleted 2026-08-07; spanning moved to src/macroembeddings/report/
/paper/
/papers/
/.superpowers/
/.pytest_cache/